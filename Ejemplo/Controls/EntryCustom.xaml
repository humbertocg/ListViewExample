<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:localization="clr-namespace:Ejemplo;assembly=Ejemplo" 
             x:Class="Ejemplo.Controls.EntryCustom"
             x:Name="Control">
    <ContentView.Content>
        <StackLayout>
            <Label Text="{Binding Source={x:Reference Control}, Path=Title}" HorizontalOptions="Start" Style="{StaticResource LabelFormTitleStyle}"/>
            <Entry Text="{Binding Source={x:Reference Control}, Path=Data}" HorizontalOptions="FillAndExpand"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>
